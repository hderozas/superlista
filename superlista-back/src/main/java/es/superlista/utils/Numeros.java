package es.superlista.utils;

/**
 * Clase de utilidades que centraliza constantes numéricas reutilizadas
 * en las entidades y validaciones del proyecto. Esto permite mantener
 * consistencia y evitar valores mágicos hardcodeados en el código.
 */
public class Numeros {

    /** Valor constante para longitud 20 (por ejemplo, en columnas de base de datos). */
    public static final int VEINTE = 20;

    /** Valor constante para longitud 50 (por ejemplo, en columnas de base de datos). */
    public static final int CINCUENTA = 50;

    /** Valor constante para longitud 100 (por ejemplo, en columnas de base de datos). */
    public static final int CIEN = 100;
}
